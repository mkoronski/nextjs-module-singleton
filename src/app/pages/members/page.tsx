'use client';

interface Member {
    id: number;
    firstName: string;
    lastName: string;
    city: string;
    timezone: string;
    languages: string[];
    teamIds: number[];
}

const MembersPage = () => {

    return (
        <div>
            <h1>Members</h1>

        </div>
    );
};

export default MembersPage;
